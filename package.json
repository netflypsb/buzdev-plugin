{
  "name": "buzdev-outreach",
  "version": "1.0.0",
  "description": "BuzDev Outreach MCP server — lead discovery, email finding, and CSV export. No external auth required.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.20.0",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}